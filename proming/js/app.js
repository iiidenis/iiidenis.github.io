

// $(function () { // когда страница загружена
//     $('.footer-style a').each(function () { // проходим по нужным нам ссылками
//         let location = window.location.href // переменная с адресом страницы
//         let link = this.href // переменная с url ссылки
//         let result = location.match(link); // результат возвращает объект если совпадение найдено и null при обратном

//         if (result != null) { // если НЕ равно null
//             $(this).addClass('current'); // добавляем класс
//         }
//     });
// });